apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: openfaas-ingress
spec:
  rules:
    - http:
        paths:
          - path: /api
            pathType: Prefix
            backend:
              service:
                name: gateway
                port:
                  number: 8080
